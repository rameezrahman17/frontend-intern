"'use client';\n\nimport React, { useEffect, useState } from 'react';\n\ntype ShapeType = 'star' | 'balloon' | 'gift' | 'sparkle';\n\ninterface PartyElement {\n  id: number;\n  left: string;\n  size: number;\n  delay: string;\n  duration: string;\n  opacity: number;\n  color: string;\n  shape: ShapeType;\n}\n\nexport default function FloatingPartyElements() {\n  const [elements, setElements] = useState<PartyElement[]>([]);\n\n  useEffect(() => {\n    // Curried harmonious pastel color palette (lavender, mint, gold, peach, soft pink)\n    const colors = [\n      '#FFD166', // Gold/Yellow\n      '#A2D2FF', // Soft Blue\n      '#BDB2FF', // Lavender\n      '#FFADAD', // Soft Red/Pink\n      '#CAFFBF', // Soft Green/Mint\n      '#FFC6FF', // Soft Magenta\n    ];\n\n    const shapes: ShapeType[] = ['star', 'balloon', 'gift', 'sparkle'];\n\n    const generatedElements: PartyElement[] = Array.from({ length: 20 }).map((_, i) => {\n      const size = Math.random() * 25 + 15; // 15px to 40px\n      const left = `${Math.random() * 100}%`;\n      const delay = `${Math.random() * 10}s`;\n      const duration = `${Math.random() * 10 + 12}s`; // 12s to 22s\n      const opacity = Math.random() * 0.25 + 0.15; // 0.15 to 0.4 opacity for visibility\n      const color = colors[Math.floor(Math.random() * colors.length)];\n      const shape = shapes[Math.floor(Math.random() * shapes.length)];\n      return { id: i, left, size, delay, duration, opacity, color, shape };\n    });\n    setElements(generatedElements);\n  }, []);\n\n  const renderShapePath = (shape: ShapeType) => {\n    switch (shape) {\n      case 'star':\n        return <path d=\"M12 .587l3.668 7.431 8.2 1.192-5.934 5.787 1.4 8.168L12 18.896l-7.334 3.857 1.4-8.168L.132 9.21l8.2-1.192z\" />;\n      case 'balloon':\n        return (\n          <>\n            {/* Balloon Body */}\n            <path d=\"M12 2a5.5 5.5 0 00-5.5 5.5c0 3.2 2 5.8 5.5 6.8v1.7l-1 .3v1h3v-1l-1-.3v-1.7c3.5-1 5.5-3.6 5.5-6.8A5.5 5.5 0 0012 2z\" />\n            {/* Balloon String */}\n            <path
<truncated 1419 bytes>